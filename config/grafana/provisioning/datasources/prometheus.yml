# Project Alexandria — Grafana Datasource Provisioning
apiVersion: 1

datasources:
  - name: Alexandria Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "30s"
      httpMethod: POST
